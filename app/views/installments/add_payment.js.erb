<% if @payment.new_record? %>
  alert('<%= j @payment.errors.full_messages.join("\n") -%>');
<% else %>
  <% if params[:button] == 'save_and_receipt' %>
    fetch('<%= receipt_path(@payment.receipt) %>');
  <% end %>
  window.location.reload()
<% end %>
